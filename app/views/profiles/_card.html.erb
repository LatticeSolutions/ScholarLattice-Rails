<%= render CardComponent.new( 
  title: "#{profile.first_name} #{profile.last_name} ⟨#{profile.email}⟩",
  description: profile.description,
  link_text: "View Profile",
  link_url: profile_path(profile),
  title_icon: profile.verified? ? :profile_verified : :profile
) %>
