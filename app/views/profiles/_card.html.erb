<%= render CardComponent.new( 
  title: "#{profile.first_name} #{profile.last_name}",
  description: "#{profile.position} @ #{profile.affiliation}",
  link_text: "View Profile",
  link_url: profile_path(profile),
  title_icon: @current_user.email == profile.email ? :profile_verified : :profile
) %>
