<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "ScholarLattice" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <%= render 'application/favicon' %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
<section class="min-h-screen bg-gray-50" x-data="{ sideBar: false }"> 
  <nav 
    class="fixed top-0 left-0 z-20 h-full pb-10 overflow-x-hidden overflow-y-auto transition origin-left transform bg-white border-r w-60 -translate-x-full"
    :class="{ '-translate-x-full' : !sideBar, 'translate-x-0' : sideBar }"
    @click.away="sideBar = false">
    <div class="px-4 py-5 flex flex-row justify-center items-center">
      <button @click.stop="sideBar = false">
        <%= render "logo" %>
      </button>
    </div>
    <nav class="text-sm font-medium text-gray-600" aria-label="Main Navigation">
      <a class="flex items-center px-4 py-3 text-gray-900 transition cursor-pointer group hover:bg-gray-100 hover:text-gray-900" 
        href="/">
        <span class="shrink-0 w-5 h-5 mr-2 text-gray-600 transition group-hover:text-gray-600">
          <%= render IconComponent.new(:home) %>
        </span>
        <span>Home</span>
      </a>
      <a class="flex items-center px-4 py-3 text-gray-900 transition cursor-pointer group hover:bg-gray-100 hover:text-gray-900"
        href="/collections">
        <span class="shrink-0 w-5 h-5 mr-2 text-gray-600 transition group-hover:text-gray-600">
          <%= render IconComponent.new(:collection) %>
        </span>
        <span>Collections</span>
      </a>
    </nav>
  </nav>
  <div class="ml-0 transition" :class="{ 'md:ml-60': sideBar }">
    <header class="flex items-center flex-row-reverse justify-between w-full px-4 bg-white border-b h-14">
      <div>
        <span class="text-gray-500 gap-3 flex items-center">
          <%= render "signin" %>
        </span>
      </div>
      <button class="block flex items-center justify-center" @click.stop="sideBar = !sideBar">
        <span class="sr-only">Menu</span>
        <span :class="{ 'hidden': sideBar }">
          <%= render "logo" %>
        </span>
        <span class="w-4 h-4">
          <%= render IconComponent.new(:hamburger) %>
        </span>
      </button>
    </header>
    <div class="p-4">
      <% if alert %><%= render partial: "alert", locals: {title: 'Notice', alert: alert} %><% end %>
      <%= yield %>
    </div>
  </div>
  <div class="fixed inset-0 z-10 w-screen h-screen bg-black bg-opacity-25 md:hidden" x-show.transition="sideBar" data-alpine-was-cloaked="" style="display: none;"></div>
</section>
  </body>
</html>
