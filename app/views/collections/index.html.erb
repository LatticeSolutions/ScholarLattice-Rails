<% content_for :title, "Collections" %>

<section class="p-8 w-full">
    <h1 class="text-2xl font-bold text-gray-900 md:text-3xl py-5 flex items-center">
        <span class="size-10">
            <%= render IconComponent.new(:collection) %>
        </span>
        <span class="px-3">Collections</span>
    </h1>

    <% if can? :create, Collection.new %>
        <%= link_to new_collection_path, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded" do %>
            <%= render IconComponent.new(:oplus) %>
            <span class="ml-2">New Collection</span>
        <% end %>
    <% end %>

    <div class="grid grid-cols-1 gap-4 lg:grid-cols-3 lg:gap-8">
        <% @collections.each do |collection| %>
            <%= render partial: "card", locals: { collection: collection, user: @current_user } %>
        <% end %>
    </div>

</section>
