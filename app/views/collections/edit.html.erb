<%= render "breadcrumb", collection: @collection %>

<section>
    <h1 class="text-2xl font-bold text-gray-900 md:text-3xl py-5">
        Edit Collection <%= @collection.short_title %>
    </h1>
    <%= render "form", collection: @collection, cancel_path: collection_path %>
</section>