<%= render partial: "breadcrumb", locals: { collection: @parent, action: "New Subcollection" } %>

<section>

    <h1 class="text-2xl font-bold text-gray-900 md:text-3xl py-5">
        New <%= @parent.subcollection_name %> for <%= @parent.short_title %>
    </h1>

<%= render "form", collection: @collection, cancel_path: collections_path %>

</section>