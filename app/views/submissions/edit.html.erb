<% content_for :title, "Edit Submission | #{@collection.title}" %>
<% content_for :breadcrumb do %>
    <%= render partial: "breadcrumb", locals: { collection: @collection, action: "Edit" } %>
<% end %>
<% content_for :description do %>
  <div class="flex items-center">
    <span class="size-8 inline-block mr-1">
      <%= render IconComponent.new(:submission) %>
    </span>
    <span>
      Edit Submission ‟<%= @submission.title %>”
    </span>
  </div>
<% end %>

<% if can? :manage, @collection %>
  <%= render "users/change_form", user: @submission.user, url: @submission, method: :patch %>
<% end %>

<%= render "form", submission: @submission, collection: @collection, current_user: @current_user, session: @session %>
