<section class="w-full">
    <h1 class="text-2xl font-bold text-gray-900 md:text-3xl py-5 flex items-center">
        <span class="size-10">
            <%= render IconComponent.new(:dashboard) %>
        </span>
        <span class="px-3">Dashboard</span>
    </h1>
    <div class="mx-auto md:w-3/4">
        <h1 class="text-xl font-bold text-gray-900 md:text-2xl py-5 flex items-center">
            <span class="size-8">
                <%= render IconComponent.new(:collection) %>
            </span>
            <span class="px-3">Collections You Admin</span>
        </h1>
        <% unless @current_user.direct_admin_collections.empty? %>
            <div class="grid grid-cols-1 gap-4 lg:grid-cols-3 lg:gap-8">
                <% @current_user.direct_admin_collections.each do |collection| %>
                    <%= render partial: "collections/card", locals: { collection: collection, admin: true } %>
                <% end %>
            </div>
        <% else %>
            <p>No collections yet!</p>
        <% end %>
        </ul>
    </div>
</section>