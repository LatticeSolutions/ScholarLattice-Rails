<% content_for :title, "New Invitation | #{@invitation.collection.title}" %>
<% content_for :breadcrumb do %>
  <%= render partial: "breadcrumb", locals: { collection: @collection, action: "New" } %>
<% end %>
<% content_for :description do %>
  <div class="flex items-center">
    <span class="size-8 inline-block mr-1">
      <%= render IconComponent.new(:invitation) %>
    </span>
    <span>
      New Invitation
    </span>
  </div>
<% end %>

  <%= render "form", invitation: @invitation %>
