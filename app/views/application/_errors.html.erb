<% if object.errors.full_messages_for(attribute).present? %>
  <ul class="text-red-700 text-sm">
    <% object.errors.full_messages_for(attribute).each do |message| %>
      <li class="p-2 bg-red-100 border-red-300 border-2 rounded-lg my-2"><%= message %></li>
    <% end %>
  </ul>
<% end %>
