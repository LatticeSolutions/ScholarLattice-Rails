<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "ScholarLattice" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="bg-white">
      <div class="mx-auto flex h-16 max-w-screen-xl items-center gap-8 px-4 sm:px-6 lg:px-8">
        <a class="block text-teal-600" href="/">
          <span class="sr-only">Home</span>
          <%= image_tag "logo.svg", class: "h-8" %>
        </a>

        <div class="flex flex-1 items-center justify-end md:justify-between">
          <nav aria-label="Global" class="hidden md:block">
            <ul class="flex items-center gap-6 text-sm">
              <li>
                <%= link_to "About", "#about" %>
              </li>
              <li>
                <%= link_to "Collections", collections_path %>
              </li>
              <li>
                <%= link_to "Profiles", "#" %>
              </li>
            </ul>
          </nav>

          <div class="flex items-center gap-4">
            <div class="sm:flex sm:gap-4">
              <%= link_to "Sign up or in", users_sign_in_path,
                class: "block rounded-md bg-blue-600 px-5 py-2.5 text-sm font-medium text-white transition hover:bg-blue-700" %>
            </div>

          </div>
        </div>
      </div>
    </header>
    <section class="overflow-hidden bg-gray-50 sm:grid sm:grid-cols-2 sm:items-center">
      <div class="p-8 md:p-12 lg:px-16 lg:py-24">
        <div class="mx-auto max-w-xl text-center ltr:sm:text-left rtl:sm:text-right">
          <h1 class="text-3xl font-bold text-gray-900 md:text-5xl py-5">
            ScholarLattice
          </h1>
          <h2 class="text-2xl font-bold text-gray-900 md:text-3xl">
            Organize your events in finite time
          </h2>

          <p class="hidden text-gray-500 md:mt-4 md:block">
            ScholarLattice is run by academics who think scholars should be able to spend
            more time on research, and less time on logistics.
          </p>

          <div class="mt-4 md:mt-8">
            <%= link_to "Sign up with just your email", users_sign_in_path,
              class: "inline-block rounded bg-blue-600 px-12 py-3 text-sm font-medium text-white transition hover:bg-blue-700 focus:outline-none focus:ring focus:ring-sky-400" %>
          </div>
        </div>
      </div>

      <%= image_tag "lecture.jpg",
        class: "h-full w-full object-cover sm:h-[calc(100%_-_2rem)] sm:self-end sm:rounded-ss-[30px] md:h-[calc(100%_-_4rem)] md:rounded-ss-[60px]" %>
    </section>
    <section class="bg-white">
  <div class="mx-auto max-w-screen-xl px-4 py-12 sm:px-6 lg:px-8 lg:py-16">
    <h2 class="text-center text-4xl font-bold tracking-tight text-gray-900 sm:text-5xl">
      Trusted by academics everywhere
    </h2>

    <div class="mt-8 grid grid-cols-1 gap-4 md:grid-cols-3 md:gap-8">
      <%= render "review" %>
      <%= render "review" %>
      <%= render "review" %>
    </div>
  </div>
</section>
  </body>
</html>
